pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'master',
                    url: 'https://github.com/JulioCesar9514/helloworld.git'
            }
        }

        stage('Instalar dependencias') {
            steps {
                bat 'pip install -r requirements.txt || true'
            }
        }

        stage('Tests') {
            steps {
                bat 'pytest || true'
            }
        }

        stage('Build') {
            steps {
                bat 'Construyendo aplicación...'
            }
        }
    }
}